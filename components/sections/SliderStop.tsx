export interface SliderStopProps {
  label: string;
  text: string;
}

export function SliderStop({ label, text }: SliderStopProps) {
  // This component is just a data holder, not rendered directly
  return null;
}
