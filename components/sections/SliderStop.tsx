export interface SliderStopProps {
  label: string;
  text: string;
  image?: string;
}

export function SliderStop({ label, text, image }: SliderStopProps) {
  // This component is just a data holder, not rendered directly
  return null;
}
